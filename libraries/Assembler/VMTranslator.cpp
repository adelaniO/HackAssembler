#include "headers/VMTranslator.h"

VMCommandType VMTranslator::Translator::commandType()
{
    return VMCommandType();
}

int VMTranslator::Translator::write(const std::filesystem::path& outputFile)
{
    return 0;
}
